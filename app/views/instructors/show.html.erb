<h1> Instructor </h1>
<%= @instructor.name%>

<h3> Students: </h3>
<ul>
    <% @instructor.sort.each do |student| %>
    <li>
        <%= link_to student.name, student %>
    </li>
    <% end %>
</ul>

<h3> Average Age </h3>
<%= @instructor.average_age %>

